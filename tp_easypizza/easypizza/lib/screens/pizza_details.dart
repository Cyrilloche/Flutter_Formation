import 'package:flutter/material.dart';

class PizzaDetails extends StatelessWidget {
  const PizzaDetails({super.key, required id});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}
